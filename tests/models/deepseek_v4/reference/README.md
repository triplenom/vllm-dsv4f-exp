# DeepSeek V4 Flash Vision-Exp reference sources

Files in this directory are verbatim copies of the official DeepSeek
reference implementation published with
`deepseek-ai/DeepSeek-V4-Flash-Vision-Exp` (Apache-2.0):

- `image_processor.py` — `inference/image_processor.py`
- `vision.py` — `inference/vision.py`

They are the specification for the parity tests in the parent directory.
Do not edit them; update them only by re-copying from the reference repo.
